/**
 * Componente vazio mantido para compatibilidade
 * As variáveis CSS foram movidas para index.css
 * @deprecated Este componente não é mais necessário, mas mantido para evitar quebras
 */
export const TrustDSStyles = () => null;